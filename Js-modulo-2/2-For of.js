const myName = 'Alex'
const users = ['Paulo','Thiago','José']


//For of, le item por item e mostra na tela, e ele para quando chega no final. Neste caso colo a variavel, e ele termina quando terminar o myName que é A L E X. e no caso de array, ele lê nome por nome. Não precisa de condição pois ele para sozinho.
for (const name of users) {
    console.log(users)
}

